<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/TwitterActionBar</item>
        <item name="android:actionBarTabBarStyle">@style/CharityChallengerTabBar</item>
        <item name="android:actionBarTabTextStyle">@style/CharityChallengerText</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="TwitterActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid">
        <item name="android:background">@color/facebook_blue</item>
        <item name="android:titleTextStyle">@style/CodePathActionBarTitleText</item>
    </style>

    <!-- ActionBar title text -->
    <style name="CodePathActionBarTitleText"
        parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="CharityChallengerTabBar" parent="android:style/Widget.Holo.Light.ActionBar.TabBar">
        <item name="android:background">@color/facebook_gary_4</item>
    </style>


    <style name="CharityChallengerText" parent="android:style/Widget.Holo.Light.ActionBar.TabBar">
        <item name="android:textColor">@color/facebook_blue</item>
    </style>
</resources>
